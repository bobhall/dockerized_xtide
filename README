*** Build the docker image
sudo docker build -t xtide .

*** Get tide info
sudo docker run xtide /xtide/xtide-2.14/tide -l "seattle"

*** Print hourly time series to file
sudo docker run xtide /xtide/xtide-2.14/tide -l "seattle" -m r -s "01:00" > out.csv

*** Plot contents of out.csv
python plot.py